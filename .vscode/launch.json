{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Pacman",
            "type": "debugpy",
            "request": "launch",
            "module": "core.game",
            "cwd": "${workspaceFolder}"
        }
    ]
}